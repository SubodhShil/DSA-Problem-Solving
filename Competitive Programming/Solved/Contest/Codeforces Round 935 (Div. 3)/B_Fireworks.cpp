#include <bits/stdc++.h>
using namespace std;
#define int long long

// not my solution
void ans()
{
    int a, b, m;
    cin >> a >> b >> m;
    // cout << ((a + m) / a) + ((b + m) / b) << endl;
}

int32_t main()
{
    int t;
    cin >> t;
    while (t--)
        ans();

    return 0;
}